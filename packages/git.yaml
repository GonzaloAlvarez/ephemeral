---
package:
  name: 'GIT Clone'
  description: 'Python GIT Native implementation'
  version: '0.0.1'
  setup:
    dependencies:
      - name: 'dulwich'
        command: '{{ pip }} install -v dulwich'
  run:
    execution:
      - command: '{{ python }} {{ bin }}/dulwich {{ args|join(' ') }}'
